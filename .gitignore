# Magelo dump files (downloaded automatically by workflow)
character/*.txt
inventory/*.txt

# Generated by extract_raid_item_sources.py (use raid_item_sources.json)
raid_item_ids.txt

# Python cache
__pycache__/
*.pyc
*.pyo

# Generated pages (workflow generates each run and deploys; not committed to avoid repo bloat)
spell_inventory.html
delta.html
delta-history.html
delta_range.html
class_rankings.html
class_rankings.json
scoring_explanation.html

# Dated leaderboards (generated each run, deployed but not committed)
leaderboard_week_*.html
leaderboard_month_*.html

# Uncompressed delta files (use .json.gz compressed versions instead)
delta_snapshots/delta_daily_*.json
!delta_snapshots/delta_daily_*.json.gz

# IDE files
.vscode/
.idea/
*.swp
*.swo
